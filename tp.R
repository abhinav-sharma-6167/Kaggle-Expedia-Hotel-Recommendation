#Setting directory
setwd("D:/Learning/Kaggle/Expedia")
library(data.table)
#Setting the seed in case for random sampling later
set.seed(100)



#freading the data into data table
book1 <- fread("book1.csv")

